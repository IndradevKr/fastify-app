
## Fastify App

To run this project, install serve package to serve front-end files:

### `npm install -g serve`

To run front-end, serve static dir using:
### `serve -p 5050 static`

To run backend, go to directory mock-serv and run:

### `npm install`

and then run server using:

### `npm run dev`
